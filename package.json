{
  "name": "positron",
  "version": "0.0.1",
  "description": "Holochain composer framework abstraction for generating web apps across electron and cordova",
  "scripts": {
    "prep": "gulp prep",
    "build-electron": "gulp build-electron",
    "build-android": "gulp build-android",
    "electron": "npm start --prefix pt-electron"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/holochain/positron.git"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "gulp": "^4.0.0",
    "gulp-cli": "^2.0.1"
  }
}
